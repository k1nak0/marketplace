# Implementation Log: task-20260308-223337

**Date:** 2026-03-08

---

## Difficulties Encountered

### 1. Feature developer introduced out-of-scope edits (Phase 5)
The `feature-developer` agent changed `model: opus` → `model: sonnet` in
`implementation-architect.md` and `README.md` without being asked to. The code
reviewer missed these because it only checked files listed in `modified-files.json`.

**Workaround:** The orchestrator reverted the out-of-scope changes via
`git checkout --` before presenting the Phase 7 diff to the human reviewer. The
reviewer then requested the opus→sonnet change as an explicit minor-fix, so the
same change was re-applied in scope with proper tracking.

### 2. Code reviewer scope limited to modified-files.json
The `code-reviewer` agent only examined files explicitly listed in `modified-files.json`,
missing changes to files outside that list. This allowed out-of-scope edits to reach
the human review gate undetected.

**Lesson learned:** The orchestrator should run `git status --short` after Phase 6
and flag any modified files not in `modified-files.json` before proceeding to Phase 7.

### 3. Rate limit interruption during Phase 4
The `implementation-architect` agent hit the API rate limit mid-execution during
Phase 4. The orchestrator resumed it via agent ID, completing successfully.

**Lesson learned:** The orchestrator's rate-limit recovery path (resume via agent ID)
works correctly; no changes needed.

### 4. README.md typo from opus→sonnet substitution
The feature developer introduced `fsonnet` (a stray character) instead of `sonnet`
in `README.md:18` during the minor-fix pass. Caught by the re-review and fixed
directly by the orchestrator with `sed`.

---

## Lessons Learned

- The orchestrator should always run `git status --short` after Phase 5 and
  cross-reference against `modified-files.json` to detect unexpected changes early.
- `AskUserQuestion` with structured options is significantly more reliable for
  phase-gate decisions than free-text prompts.
