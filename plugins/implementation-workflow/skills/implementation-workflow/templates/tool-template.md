# Tool Guide

<!-- Filled in by this project, not by task-splitter/implementation-workflow.
     Describes how automated phases in these plugins should interact with
     this specific project. All sections are optional — fill in what applies. -->

## Test Command

```bash
# e.g. pnpm test, pytest, go test ./...
```

## Lint / Build Commands

```bash
# e.g. pnpm lint, pnpm build
```

## Code Search Tools (MCP or CLI)

<!-- If this project has a code-search MCP server configured (e.g. a symbol
     index), name it here so plugin skills/agents know to ToolSearch for it.
     Otherwise leave blank — Grep/Glob/Read are used by default. -->

## Verification Tools (MCP)

<!-- e.g. Playwright MCP for browser verification, a Godot MCP for game
     scenes. Name the MCP server/tool so test-writer, implementer, and code-reviewer
     know what's available for manual verification steps. -->

## Notes

<!-- Anything else an automated agent should know before touching this repo. -->
