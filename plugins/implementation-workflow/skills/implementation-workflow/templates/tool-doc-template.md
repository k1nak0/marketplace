# <Tool Name>

<!-- Filled in by this project, not by implementation-workflow. One file per
     external tool (MCP server, CLI, etc.) available to Claude Code in this
     repository. This file describes the tool itself and nothing else — it
     does not say which agent should use it. That belongs in the consuming
     agent's own docs/tools/<agent-name>.md, which links back here. -->

## What It Is

<!-- One or two sentences: what this tool does. -->

## How to Reach It

<!-- MCP registration (scope, transport, e.g. ".mcp.json" project-scope
     stdio/HTTP), the exact ToolSearch name, or a plain CLI invocation.
     Include anything non-obvious: a required background process, an env
     var, an auth step. -->

## Notes

<!-- Gotchas, version pins, one-time setup a fresh clone needs before this
     tool works. -->
