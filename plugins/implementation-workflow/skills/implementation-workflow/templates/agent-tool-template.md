# Tools for <agent-or-skill-name>

<!-- Filled in by this project, not by implementation-workflow. One file per
     agent/skill that has a real reason to reach for a specific tool.
     Written from this consumer's point of view — which tool(s) it should
     consider and when/how — not a description of the tool itself (that's
     docs/tools/<tool-slug>.md, linked below). Leave this file absent
     entirely if this consumer has no tool to name; do not create an empty
     placeholder. -->

## <Situation this consumer faces, e.g. "Investigating library documentation">

Consider [`<tool-name>`](<tool-slug>.md) before falling back to
<the plugin's built-in default for this situation — Grep/Glob/Read,
WebSearch/WebFetch, trusting a recorded observation, etc.>.

<!-- Repeat this section once per tool/situation relevant to this consumer. -->
